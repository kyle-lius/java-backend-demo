<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.backenddemo.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.example.backenddemo.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, product_id, product_name, price, quantity, subtotal, create_time, update_time
    </sql>

    <select id="selectByOrderId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM order_item
        WHERE order_id = #{orderId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByOrderIdAndProductId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM order_item
        WHERE order_id = #{orderId} AND product_id = #{productId}
    </select>

    <insert id="batchInsert">
        INSERT INTO order_item (order_id, product_id, product_name, price, quantity, subtotal, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price, jdbcType=DECIMAL}, #{item.quantity}, #{item.subtotal, jdbcType=DECIMAL}, NOW(), NOW())
        </foreach>
    </insert>

    <delete id="deleteByOrderId" parameterType="java.lang.Long">
        DELETE FROM order_item
        WHERE order_id = #{orderId}
    </delete>

    <select id="countByOrderId" parameterType="java.lang.Long" resultType="int">
        SELECT COUNT(*) FROM order_item
        WHERE order_id = #{orderId}
    </select>
</mapper>